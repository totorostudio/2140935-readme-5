import { SortDirection, SortBy } from '@project/libs/shared/app/types';

export const DEFAULT_POST_COUNT_LIMIT = 10;
export const DEFAULT_POST_SEARCH_COUNT_LIMIT = 10;
export const DEFAULT_SORT_DIRECTION = SortDirection.Desc;
export const DEFAULT_SORT_BY = SortBy.CreatedAt;
export const DEFAULT_PAGE_COUNT = 1;
